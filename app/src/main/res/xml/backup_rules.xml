<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude Room DB and prefs from auto backup — app has its own manual export -->
    <exclude domain="database" path="."/>
    <exclude domain="sharedpref" path="."/>
</full-backup-content>
